pub fn visible_function() -> &'static str {
    "local structural analysis remains available"
}

// Deliberately fake secret-like fixture used to verify line filtering.
const TOKEN: &str = "TOKEN=V07-FAKE-FILTER-MARKER";
