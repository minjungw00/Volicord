# Polyglot greeting service

The Java gateway, Python formatter, and TypeScript client exchange a greeting
through a documented JSON-shaped process boundary. This document is evidence,
not a syntax-level call relation between languages.
