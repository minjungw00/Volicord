# Conformance Reference

## 1. Current status

This repository is documentation-only and still in documentation review. No Harness Server runtime, conformance runner, executable fixture files, generated conformance reports, generated runtime artifacts, or current runtime conformance results exist here.

This document is not an executable conformance suite. It owns current documentation criteria for conformance meaning, candidate future fixture shape, assertion authority, and compact representative examples. Current phase and handoff status remain owned by [MVP Plan](../build/mvp-plan.md#documentation-acceptance-status).

| Item | Current status | Owner | Executable now? |
|---|---|---|---|
| current documentation criteria | active reference criteria | `docs/en/reference/conformance.md` | no runtime execution |
| planned internal smoke target | planned/documented | `docs/en/build/mvp-plan.md` | no |
| future fixture shape | candidate format | `docs/en/reference/conformance.md` | no |
| future executable fixtures | not implemented | future runner and owner-promoted fixtures | no |
| later conformance reporting | later candidate | `docs/en/later/index.md` | no |

When this page says "must", "required", or "always", it is naming the current documentation criterion or a requirement for a future server/runner after implementation exists. It is not a claim that this repository already contains executable checks.

## 2. What conformance means

For a future server, conformance means executable checks can compare a specific owner-defined action with owner-defined authority records. A future check drives one Core, API, or operator action, captures response facts and owner-state effects, and compares them with structured expectations, including forbidden side effects that must remain absent.

Documentation checks are separate. Markdown maintenance checks inspect links, terminology, owner boundaries, active/later wording, security wording, and bilingual parity. They are current documentation aids only, not runtime conformance.

A future runtime conformance check must not judge generated prose, agent summaries, rendered reports, or status wording. It may judge only the facts that an owning document has made authoritative.

## 3. What does not exist yet

The following are future implementation work, not current repository contents:

- Harness Server runtime or Harness Runtime Home data
- executable fixture files or a fixture directory
- a conformance runner or `harness conformance run` implementation
- generated conformance reports, generated runtime artifacts, projections, operational files, or runtime state
- current runtime results for active MVP behavior or later candidates
- current `changed_path_detection_verification=passed` result for the baseline `reference-local-mcp` surface
- current runtime proof of preventive blocking, OS permission control, arbitrary-tool sandboxing, tamper-proof storage, security isolation, or profile-gated `preventive` / `isolated` guarantee claims

Examples on this page may guide planning, but they do not create runtime state, acceptance evidence, close readiness, residual-risk acceptance, generated reports, or implementation readiness.

## 4. Fixture shape

Fixture shape is a candidate future format, not current files. After the Harness Server and runner exist, a promoted fixture should be a compact structured record with these parts:

| Part | Purpose |
|---|---|
| `scenario_id` | Stable identifier for the behavior under review. |
| authority context | The Task, Change Unit, state version, surface, owner refs, Core state, storage rows, artifact refs, and capability facts needed before the action. |
| action | One public Core, API, or operator request using the owner request schema. |
| expected assertions | Structured response facts, owner-state effects, storage or artifact facts, blocker facts, error facts, guarantee-display facts, and required absence of forbidden side effects. |
| owner links | The API, Core, Storage, Security, Agent Integration, ArtifactRef, and policy owners that define exact values and meaning. |

A future materialized fixture must use public owner schemas. It must not invent fixture-only enum values, pseudo-fields, localized display labels as state, prose-only expectations, or later-candidate-only values.

## 5. Assertion authority

Assertion authority is the narrow set of facts a future fixture may judge after executable fixtures exist. Authority comes from owner-defined facts, not from scenario prose or generated summaries.

Authoritative future assertions may use:

- response facts returned by public owner APIs
- Core-owned Task, Change Unit, user judgment, Write Authorization, Run or evidence summary, blocker, close, and residual-risk state
- Storage-owned row effects, idempotency/replay facts, project-wide `project_state.state_version` facts, and artifact-integrity facts when artifacts are in scope
- request-level `VerifiedSurfaceContext.access_class` facts, with exactly one access class per public API request
- temporary `StagedArtifactHandle` response facts, including server-recorded `created_by_surface_id` and `created_by_surface_instance_id`, only where the `harness.stage_artifact` owner is in scope, with persistent artifact authority asserted only after compatible `record_run` promotion validates those provenance fields against the current verified `surface_id` and `surface_instance_id`
- stable `task_events` only after the Core owner promotes event names
- primary `ErrorCode`, structured blocker fields, and guarantee-display facts that match the API, Core, Security, and Agent Integration owners
- absence assertions for forbidden side effects, such as no durable authorization, no Run row, no artifact mutation, or no close-state change

As current documentation criteria, active examples may name `cooperative` and supported `detective` facts. For a future server/runner on the baseline `reference-local-mcp` surface, `detective` assertions are valid only when `changed_path_detection_verification=passed` and only for the verified changed-path detection scope. `not_run`, legacy `planned_not_run` wording, `failed`, and `stale` are not passing assertion states and cannot justify a `detective` label. `preventive` or `isolated` assertions are valid only for a promoted profile with an owner-defined proof path for that profile; conformance planning text does not make those display values currently executable or proven.

For future conformance, non-authoritative material includes prose scenario descriptions, comments, author notes, rendered Markdown, generated reports, status prose, agent summaries, documentation-check labels, and projections. Projection freshness or availability may be asserted only when projection support is explicitly in scope.

## 6. Representative active examples

These are compact current documentation criteria for future server behavior. They are not fixture files, full YAML bodies, current runtime results, a complete conformance suite, generated runtime objects, or an implementation plan. The response-branch expectations in these rows are documentation-level conformance examples; they do not create executable fixtures, test runners, generated operational files, or runtime state. The first internal documentation smoke target in [MVP Plan](../build/mvp-plan.md#first-internal-smoke-target) may draw from these rows, but future executable coverage still needs owner-promoted fixtures and a runner.

These examples keep the blocker-like response structures separate. Pre-commit failures use `ToolRejectedResponse.errors: ToolError[]`, with `errors[0].code` carrying the relevant `ErrorCode`; they use no `WriteDecisionReason`, `CloseReadinessBlocker`, or `PlannedBlocker`, and create no replay row or `project_state.state_version` increment. Non-allowed `prepare_write` results use `PrepareWriteResult.write_decision_reasons: WriteDecisionReason[]`; they use no `CloseReadinessBlocker` and do not mutate `close_state`. Read-only status and close-check results use `StatusResult.close_blockers: CloseReadinessBlocker[]` or `CloseTaskResult.blockers: CloseReadinessBlocker[]` only as read-only observation/evaluation data. Committed blocked `close_task` results use `CloseTaskResult(close_state=blocked).blockers: CloseReadinessBlocker[]` only for semantic close-readiness findings after preflight succeeds. Valid dry-run previews use `DryRunSummary.would_blockers: PlannedBlocker[]` with `PlannedBlocker.source_kind=write_decision` or `source_kind=close_readiness`; dry-run preview does not store or return real/current `WriteDecisionReason` or `CloseReadinessBlocker` objects. `STATE_VERSION_CONFLICT` is not `WriteDecisionReason.code`, not `CloseReadinessBlocker.code`, and not `PlannedBlocker.code`. `CloseBlocker` is not an active current-MVP conformance type.

| Example | Current documentation criterion for a future server | Future fixture assertion focus |
|---|---|---|
| `MVP-ACTIVE-registered-surface-mismatch-blocks-mutation` | A mutating request whose `surface_id` selects a stored `LocalSurfaceRegistration` but whose reachable transport, session, or binding does not match must stop before commit. | Expected response branch: `ToolRejectedResponse` with `errors[0].code=LOCAL_ACCESS_MISMATCH`. No owner record, event, replay row, Write Authorization, Run, artifact, close effect, or `project_state.state_version` increment is created. A copied `surface_id` is not authority. |
| `MVP-ACTIVE-verified-local-surface-allows-owner-mutation` | A same-project active local registration can allow mutation only when the server derives `VerifiedSurfaceContext.verified=true` for the method's required `access_class`. | The request proceeds to method-specific scope, judgment, artifact, close, and current `ToolEnvelope.expected_state_version` checks; if those owner checks pass, the committed mutation creates only method-owned effects and increments `project_state.state_version` exactly once. Surface verification alone creates no scope, approval, evidence, Write Authorization, or close state. |
| `MVP-ACTIVE-single-access-class-per-public-request` | The active MVP evaluates one request-level `VerifiedSurfaceContext.access_class` per public API request. | `harness.record_run` requires `run_recording` only, including when `ArtifactInput[]` contains `source_kind=staged_artifact`; `harness.stage_artifact` requires `artifact_registration` only; artifact body reads require `artifact_read`. No assertion may model one public request as requiring both `run_recording` and `artifact_registration`. |
| `MVP-ACTIVE-detective-display-capability-gated` | A `detective` guarantee display is valid only after the relevant active capability check has passed for the covered observable scope. | For baseline `reference-local-mcp`, `changed_path_detection_verification=passed` is required and applies only to verified changed-path detection. `not_run`, legacy `planned_not_run`, `failed`, or `stale` leaves the display `cooperative` or returns `CAPABILITY_INSUFFICIENT` when the stronger capability is required; no command, network, secret, artifact-capture, blocking, or isolation fact is marked verified. |
| `MVP-ACTIVE-shaping-readiness-gap-blocks-or-asks` | A `ShapingReadiness` read can expose an incomplete goal, scope, Change Unit, Autonomy Boundary, named user-owned blocker, or next safe action without creating persistent planning artifacts. | Missing readiness returns an active blocker, `StateSummary.shaping_readiness` gap, or pending `UserJudgment` candidate according to the owner path; no Discovery Brief, Question Queue, Assumption Register, evidence, final acceptance, residual-risk acceptance, close state, or later planning artifact is created by the read. |
| `MVP-ACTIVE-project-state-version-stale-mutation-rejected` | Given a state-changing non-dry-run request whose `ToolEnvelope.expected_state_version` is stale relative to current project-wide `project_state.state_version`, the request fails before commit. `tasks.state_version` is not an active conflict or concurrency basis. | Expected response branch: `ToolRejectedResponse` with `errors[0].code=STATE_VERSION_CONFLICT`. The failed attempt creates no `WriteDecisionReason`, `CloseReadinessBlocker`, or `PlannedBlocker`, no current records, events, artifacts, evidence, Write Authorization creation or consumption, close state, replay rows, or state-version increments. Fresh committed mutations increment project-wide `project_state.state_version` by exactly 1. |
| `MVP-ACTIVE-dry-run-pre-commit-failure-rejected` | A request with `dry_run=true` still fails before any read-only result or dry-run preview when validation, local access, capability, state lookup, stale state, stale Write Authorization basis, or staged-handle validation fails. | Expected response branch: `ToolRejectedResponse` with `dry_run=true`, `effect_kind=no_effect`, and `errors[0].code` carrying the relevant pre-commit `ErrorCode`; it is not `ToolDryRunResponse` and not a method-specific `MethodResult`. The rejection creates no `WriteDecisionReason`, `CloseReadinessBlocker`, or `PlannedBlocker`, no current record, `task_events`, replay row, artifact, evidence, close state, staged-handle consumption, Write Authorization creation or consumption, or `project_state.state_version` increment. |
| `MVP-ACTIVE-status-close-blockers-read-only` | `harness.status` with close information requested may compute current close readiness and return close blockers without changing state. | Expected response branch: `StatusResult` as a read-only result with `base.effect_kind=read_only` and `StatusResult.close_blockers: CloseReadinessBlocker[]`. With `dry_run=true`, the result remains `StatusResult` with `base.dry_run=true` and `base.effect_kind=read_only`. No stored blocker row, `task_event` or `task_events` append, replay row, `tool_invocations.response_json`, `close_state` mutation, Write Authorization change, staged-handle consumption, artifact effect, evidence update, or `project_state.state_version` increment occurs. |
| `MVP-ACTIVE-sensitive-approval-records-sensitive-action-scope` | Sensitive-action approval is recorded as a `judgment_kind=sensitive_approval` user judgment with `SensitiveActionScope`, separate from path-level `AuthorizedAttemptScope`. | The recorded scope names the sensitive action and honest `SensitiveActionScope.capability_claim`; it does not create Write Authorization, evidence, final acceptance, residual-risk acceptance, OS enforcement, sandboxing, blocking, isolation, or artifact authority. Product-file Write Authorization still requires the owner `prepare_write` path. |
| `MVP-ACTIVE-prepare-write-requires-compatible-scope-and-approval` | `harness.prepare_write` may return `decision=allowed` only when the intended product-file write is compatible with the active Task, Change Unit, scope, baseline, Autonomy Boundary, required user judgment, required separate sensitive-action approval, surface capability, and current project-wide state version. | Expected response branch for a committed non-allowed outcome: `PrepareWriteResult` with `decision=blocked`, `decision=approval_required`, or `decision=decision_required`. `write_decision_reasons: WriteDecisionReason[]` is non-empty, and `WriteDecisionReason.code != STATE_VERSION_CONFLICT`. `CloseReadinessBlocker` is not used, `close_state` is not mutated, close-readiness evaluation does not run, and no `close_task` effect occurs. Project-wide version mismatch uses `ToolRejectedResponse` with `errors[0].code=STATE_VERSION_CONFLICT`, not a `PrepareWriteResult.decision` value. Non-allowed outcomes create no consumable Write Authorization, Run, artifact, evidence, final-acceptance, residual-risk, or close state. A valid `dry_run` preview uses `DryRunSummary.would_blockers: PlannedBlocker[]` with `source_kind=write_decision`, stores no real/current `WriteDecisionReason` or `CloseReadinessBlocker`, and keeps `PlannedBlocker.code != STATE_VERSION_CONFLICT`. |
| `MVP-ACTIVE-authorized-attempt-scope-product-file-write-only` | `AuthorizedAttemptScope` is only the path-level product-file write-attempt scope stored by an allowed `prepare_write` and later compared by `harness.record_run`. | The scope covers Task, Change Unit, project-wide `basis_state_version`, `surface_id`, intended product-file paths, product-write sensitive categories, baseline, related user judgment refs, and honest `guarantee_level`. Command execution, dependency installation, network effects, secret access, deployment, destructive action, system access, native artifact capture, pre-tool blocking, and isolation are not stored as `AuthorizedAttemptScope` fields; attempts to represent them as verified write scope are rejected or blocked without creating authorization. |
| `MVP-ACTIVE-record-run-consumes-write-authorization-once` | A compatible product-write `harness.record_run` consumes the matching active Write Authorization exactly once. | The Run links to one compatible Write Authorization and sets that authorization consumed by the Run; idempotent replay returns the original response without consuming again. A Write Authorization whose project-wide basis is stale returns `ToolRejectedResponse.errors[0].code=STATE_VERSION_CONFLICT`; missing authorization uses `errors[0].code=WRITE_AUTHORIZATION_REQUIRED`; expired, revoked, consumed, incompatible, or observed-outside-authorized-scope authorization attempts use the applicable owner `ErrorCode`. None of these failed attempts creates a successful Run, evidence, artifact promotion, close state, Write Authorization consumption, or state-version increment. |
| `MVP-ACTIVE-stage-artifact-temporary-handle-only` | Given a verified local surface with `VerifiedSurfaceContext.access_class=artifact_registration`, successful `harness.stage_artifact` stages caller-provided safe bytes or a safe notice as a temporary same-project same-Task `StagedArtifactHandle`. | When staging succeeds, the response and staging state record `created_by_surface_id` and `created_by_surface_instance_id` from the successful request's `VerifiedSurfaceContext`; those fields are not submitted by the user as authority claims. No persistent `ArtifactRef` exists yet, and evidence sufficiency does not change yet. Staging creates no Core record, evidence summary, blocker, event, `tool_invocations` replay row, close effect, or `project_state.state_version` increment. The handle remains scoped, expiring, and single-consumption; it is not evidence authority, arbitrary local-file authority, or a bearer token. Projection files, generated Markdown, chat text, Product Repository files, and agent memory cannot create this provenance. |
| `MVP-ACTIVE-record-run-artifact-input-validation-order` | `harness.record_run` validates artifact inputs in a deterministic method order before commit. | The order is request-level `VerifiedSurfaceContext.access_class=run_recording`, project-wide `ToolEnvelope.expected_state_version`, referenced Task and Change Unit, compatible Write Authorization when product file writes are recorded, `staged_artifact` handle validation, staged-handle `project_id`/`task_id`/`created_by_surface_id`/`created_by_surface_instance_id`/expiration/consumed/`sha256`/`size_bytes`/`redaction_state` checks, staged promotion, staged consumption, `existing_artifact` validation as a same-project allowed-scope persistent `ArtifactRef`, and no artifact body read. Artifact body reads require `artifact_read`. |
| `MVP-ACTIVE-record-run-promotes-staged-artifact-to-artifact-ref` | Given a request verified with `VerifiedSurfaceContext.access_class=run_recording` and a staged handle created for the same `project_id`, `task_id`, server-recorded `created_by_surface_id`, and server-recorded `created_by_surface_instance_id`, where the handle is not expired, not consumed, and its `sha256` and `size_bytes` match, `harness.record_run` may consume that handle. | `record_run` succeeds, marks the staged handle consumed in the same committing transaction, creates a persistent `ArtifactRef` whose metadata matches the staged `sha256`, `size_bytes`, `content_type`, `redaction_state`, project, Task, and server-recorded creation surface provenance, and links the Run evidence to the new `ArtifactRef`. This promotion uses `run_recording`; it does not require `artifact_registration`, which belongs only to the earlier `harness.stage_artifact` request. |
| `MVP-ACTIVE-record-run-rejects-staged-artifact-surface-instance-mismatch` | Given a request verified with `VerifiedSurfaceContext.access_class=run_recording` and an otherwise existing staged handle whose server-recorded `created_by_surface_id` or `created_by_surface_instance_id` differs from the current verified `surface_id` or `surface_instance_id`, `harness.record_run` fails before staged promotion. | Expected response branch: `ToolRejectedResponse` with `errors[0].code=VALIDATION_FAILED` and `ToolError.details.artifact_input_error.reason=staged_handle_surface_mismatch`. It is not `LOCAL_ACCESS_MISMATCH` or `CAPABILITY_INSUFFICIENT` unless request-level surface verification or capability checking itself failed. The rejection creates no `run_summary`, Run, persistent `ArtifactRef`, evidence update, Write Authorization consumption, replay row, staged-handle consumption, or state-version increment. |
| `MVP-ACTIVE-record-run-links-existing-artifact-without-registering-bytes` | Given `ArtifactInput.source_kind=existing_artifact`, the referenced `ArtifactRef` must already be persistent and valid for the same project and allowed Task scope. | `record_run` links the existing `ArtifactRef` into the Run evidence; it does not register new artifact bytes, stage new bytes, consume a `StagedArtifactHandle`, or read artifact body content. Invalid or out-of-scope existing artifacts fail before commit with `ToolRejectedResponse.errors[0].code` set to the owner-selected relevant `ErrorCode`, without creating artifact, evidence, replay, or state-version effects. |
| `MVP-ACTIVE-captured-artifact-rejected-in-active-mvp` | `captured_artifact`, captured handles, native artifact capture, raw capture-adapter output, raw filesystem paths, and raw logs are not active MVP artifact authority. | `ArtifactInput.source_kind` accepts only `staged_artifact` or `existing_artifact`. A submitted `captured_artifact` source shape is rejected before mutation with `ToolRejectedResponse.errors[0].code=VALIDATION_FAILED`; requests that depend on unavailable native capture capability use the owner capability path. No Run, persistent `ArtifactRef`, artifact link, evidence summary, close effect, or state-version increment is created. |
| `MVP-ACTIVE-close-task-complete-stale-state-version-rejected` | Given `harness.close_task intent=complete` with stale `ToolEnvelope.expected_state_version` relative to current project-wide `project_state.state_version`, close preflight fails before the complete close-readiness evaluation runs. | Expected response branch: `ToolRejectedResponse` with `effect_kind=no_effect` and `errors[0].code=STATE_VERSION_CONFLICT`, not `CloseTaskResult`. The rejected attempt creates no `WriteDecisionReason`, no `CloseReadinessBlocker`, no `PlannedBlocker`, no `task_event` or `task_events` append, no replay row, no `tool_invocations.response_json`, no `close_state` mutation, no Write Authorization creation or consumption, no staged-handle consumption, no artifact promotion or link, no evidence update, and no `project_state.state_version` increment. |
| `MVP-ACTIVE-close-task-complete-stale-write-authorization-basis-rejected` | Given `harness.close_task intent=complete` whose close preflight finds a close-relevant `WriteAuthorization.basis_state_version` stale against current `project_state.state_version`, the attempt fails before any close-readiness commit or Write Authorization consumption. | Expected response branch: `ToolRejectedResponse` with `effect_kind=no_effect` and `errors[0].code=STATE_VERSION_CONFLICT`, not `CloseTaskResult`. The rejected attempt creates no `WriteDecisionReason`, no `CloseReadinessBlocker`, no `PlannedBlocker`, no `task_event` or `task_events` append, no replay row, no `tool_invocations.response_json`, no `close_state` mutation, no Write Authorization creation or consumption, no staged-handle consumption, no artifact promotion or link, no evidence update, and no `project_state.state_version` increment. |
| `MVP-ACTIVE-close-task-blocks-current-write-compatibility` | Given `harness.close_task intent=complete` after preflight has established current `ToolEnvelope.expected_state_version` and any close-relevant `WriteAuthorization.basis_state_version`, the complete close-readiness evaluation can still find a semantic Write Authorization or product-write Run incompatibility. | Expected response branch: `CloseTaskResult` with `CloseTaskResult.close_state=blocked` and `CloseTaskResult.blockers: CloseReadinessBlocker[]`. The blocker uses `CloseReadinessBlocker.category=write_compatibility` and `CloseReadinessBlocker.code != STATE_VERSION_CONFLICT`. `CloseReadinessBlocker.code` must use a semantic owner code such as `WRITE_AUTHORIZATION_REQUIRED`, `WRITE_AUTHORIZATION_INVALID`, `SCOPE_VIOLATION`, `BASELINE_STALE`, or `VALIDATOR_FAILED`. Any committed blocked effects are limited to API/storage-allowed blocker-state, `task_events`, replay row, and `project_state.state_version` effects; the Task remains open. |
| `MVP-ACTIVE-close-task-blocks-evidence-insufficient` | `harness.close_task intent=complete` blocks when required evidence is absent, partial, stale, blocked, or otherwise insufficient under the active `CompletionPolicy`. | Expected response branch: `CloseTaskResult` with `CloseTaskResult.close_state=blocked` and `CloseTaskResult.blockers: CloseReadinessBlocker[]`. The blocker uses `CloseReadinessBlocker.category=evidence`, `CloseReadinessBlocker.code != STATE_VERSION_CONFLICT`, and optional primary error `EVIDENCE_INSUFFICIENT`. Any committed blocked effects are limited to API/storage-allowed blocker-state, `task_events`, replay row, and `project_state.state_version` effects; the Task remains open. Final acceptance and residual-risk acceptance cannot make missing required `EvidenceCoverageItem.required_for_close=true` coverage sufficient. |
| `MVP-ACTIVE-close-task-blocks-required-artifact-unavailable` | `harness.close_task intent=complete` blocks when a close-relevant required `ArtifactRef` is missing, unavailable, integrity-failed, blocked beyond the allowed safe notice, or unusable. | Expected response branch: `CloseTaskResult` with `CloseTaskResult.close_state=blocked` and `CloseTaskResult.blockers: CloseReadinessBlocker[]`. The blocker uses `CloseReadinessBlocker.category=artifact_availability`, `CloseReadinessBlocker.code != STATE_VERSION_CONFLICT`, and optional primary error `ARTIFACT_MISSING`. Any committed blocked effects are limited to API/storage-allowed blocker-state, `task_events`, replay row, and `project_state.state_version` effects; the Task remains open and no terminal close state is committed. Evidence sufficiency and artifact availability remain distinct, and final acceptance or residual-risk acceptance does not replace a required artifact. |
| `MVP-ACTIVE-close-task-blocks-final-acceptance-missing` | `harness.close_task intent=complete` blocks when required `final_acceptance` is missing, rejected, stale, or not tied to the visible close basis after evidence and close-relevant artifacts have passed. | Expected response branch: `CloseTaskResult` with `CloseTaskResult.close_state=blocked` and `CloseTaskResult.blockers: CloseReadinessBlocker[]`. The blocker uses `CloseReadinessBlocker.category=final_acceptance`, `CloseReadinessBlocker.code != STATE_VERSION_CONFLICT`, and optional primary error `ACCEPTANCE_REQUIRED`; the next action routes through the active `final_acceptance` judgment path when a user judgment is needed. Any committed blocked effects are limited to API/storage-allowed blocker-state, `task_events`, replay row, and `project_state.state_version` effects; the Task remains open and acceptance is not inferred from evidence, status text, chat, or residual-risk acceptance. |
| `MVP-ACTIVE-close-task-blocks-visible-unaccepted-residual-risk` | `harness.close_task intent=complete` blocks when close-affecting residual risk is visible but lacks compatible `residual_risk_acceptance`. | Expected response branch: `CloseTaskResult` with `CloseTaskResult.close_state=blocked` and `CloseTaskResult.blockers: CloseReadinessBlocker[]`. The blocker uses `CloseReadinessBlocker.category=residual_risk_acceptance`, `CloseReadinessBlocker.code != STATE_VERSION_CONFLICT`, and `DECISION_REQUIRED` or `DECISION_UNRESOLVED`; the next action routes through `judgment_kind=residual_risk_acceptance` when a user judgment is needed. Any committed blocked effects are limited to API/storage-allowed blocker-state, `task_events`, replay row, and `project_state.state_version` effects; the Task remains open. This case is distinct from `RESIDUAL_RISK_NOT_VISIBLE`, which applies when the risk is not visible enough for the user to judge. |
| `MVP-ACTIVE-close-task-check-read-only` | `harness.close_task intent=check` computes close readiness and blockers without changing state, including when the request has `dry_run=true`. | Expected response branch: `CloseTaskResult` as a read-only result with `base.effect_kind=read_only` and `CloseTaskResult.blockers: CloseReadinessBlocker[]`; with `dry_run=true`, it remains `CloseTaskResult` with `base.dry_run=true` and `base.effect_kind=read_only`, not `ToolDryRunResponse`. No stored blocker row, `task_event` or `task_events` append, replay row, `tool_invocations.response_json`, `close_state` mutation, evidence summary, artifact effect, staged-handle consumption, Write Authorization creation or consumption, or `project_state.state_version` increment occurs. |
| `MVP-ACTIVE-close-task-state-effecting-dry-run-preview` | `harness.close_task` is a mixed intent method: response branch selection follows the selected intent's state effect, not the method name alone. For `intent=complete`, `intent=cancel`, or `intent=supersede` with `dry_run=true`, an otherwise valid and previewable request selects the state-effecting dry-run branch. | Expected response branch: `ToolDryRunResponse` with `effect_kind=no_effect` for those valid state-effecting dry-run previews. The preview describes would-be close blockers through `DryRunSummary.would_blockers: PlannedBlocker[]` with `source_kind=close_readiness`; it does not store or return real/current `CloseReadinessBlocker` or `WriteDecisionReason` objects, and `PlannedBlocker.code != STATE_VERSION_CONFLICT`. It creates no close-state mutation, `task_events`, replay row, artifact creation/update/linking, staged-handle consumption, Write Authorization creation or consumption, or `project_state.state_version` increment. |
| `MVP-ACTIVE-close-task-supersede-one-state-version` | `close_task intent=supersede` is a terminal non-completion path that may update the old Task and the active Task pointer together. | A valid supersede stores `Task.lifecycle_phase=superseded`, `Task.close_reason=superseded`, `Task.result=superseded`, and updates `project_state.active_task_id` to a valid open same-project `superseding_task_id` or clears it when the owner rules require clearing. The combined lifecycle and pointer change is one project-wide state mutation with exactly one `project_state.state_version` increment; invalid supersession returns the applicable blocker without hiding evidence, acceptance, or residual-risk requirements for completion. |

## 7. Catalog-only future boundary

Future fixture families belong in [Later Candidate Index: Future Fixture Families](../later/index.md#future-fixture-families). That index keeps names only as later candidates, and this page does not reproduce the catalog.

Future-family names are not scenario scripts, fixture bodies, active API payload examples, runner or reporting requirements, active MVP scope, implementation tasks, current results, or current runtime proof. A future owner must promote a narrow behavior with scope, fallback behavior, exact contracts, and proof-path expectations before executable fixture material exists.

## 8. Metrics boundary

Metrics are not conformance authority in the current documentation set. Future local metrics and later conformance reporting may be useful for diagnostics or planning, but until an owner promotes them they remain read-only derived displays or later candidates.

Metrics must not create Core state, satisfy evidence, pass QA or verification, authorize writes, accept final results, accept residual risk, close work, prove implementation readiness, or replace runtime conformance. If a future metric is promoted, its owner must define source records, freshness boundary, display wording, and the non-substitution rule.
