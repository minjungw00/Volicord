# 구현 설계 패턴

이 가이드는 현재 Rust 워크스페이스에서 보이는 안정적인 구현 구조를
설명합니다. 아키텍처 가이드 문서이므로 문제, 소스 위치, 장점, 고려할 점,
테스트를 이름으로 가리키지만 공개 API 동작, 스키마, 저장 효과, 보안
보장, 런타임 동작, Core 권한 의미를 정의하지 않습니다.

여기서 "닮았다", "기능한다", "여기서는 ...로 쓰인다" 같은 표현을
의도적으로 사용합니다. 소스는 오래 유지되는 구조를 보여 주지만, 이
페이지는 코드가 보여 주는 것 이상의 공식 패턴 의도를 주장하지 않습니다.

정확한 제품 동작은 [참조 색인](../reference/README.md)을 사용합니다.
넓은 크레이트 지도는 [구현 아키텍처](architecture.md)를, 대표 메서드
흐름은 [요청 생명주기](request-lifecycle.md)를 사용합니다.

## 패턴 지도

| 구조 | 해결하는 문제 | 소스 위치와 심볼 | 장점 | 고려할 점 | 테스트 |
|---|---|---|---|---|---|
| 포트와 어댑터를 닮은 의존 방향 | 공개 메서드 동작이 전송 계층과 설정 진입점에서 독립적으로 유지되어야 합니다. | [`crates/volicord-core/src/pipeline.rs`](../../../crates/volicord-core/src/pipeline.rs)의 `CoreService`, [`crates/volicord-mcp/src/adapter.rs`](../../../crates/volicord-mcp/src/adapter.rs)의 `McpAdapter`, `volicord-core`, `volicord-mcp`, `volicord-cli` Cargo 매니페스트. | Core를 직접 실행하거나 MCP를 통해 실행해도 어댑터가 메서드 의미를 소유하지 않습니다. | 어댑터 시작과 세션 검증에는 Store 접근이 필요하므로, 시작 검증과 공개 메서드 실행을 구분해서 읽어야 합니다. | [`crates/volicord-core/src/methods/tests/status.rs`](../../../crates/volicord-core/src/methods/tests/status.rs)의 `status_is_read_only_including_dry_run`, [`crates/volicord-mcp/src/tests/tool_calls.rs`](../../../crates/volicord-mcp/src/tests/tool_calls.rs)의 `mcp_status_does_not_advance_state_version`, [`tests/integration/mcp_connection.rs`](../../../tests/integration/mcp_connection.rs)의 `connection_invocation_is_injected_and_single_project_is_auto_selected`. 이 테스트들은 전체 응답 동등성이 아니라 각 계층의 속성을 따로 확인합니다. |
| 애플리케이션 서비스처럼 기능하는 `CoreService` | 공개 메서드가 Runtime Home, ID 생성, 시간, Store 접근, 응답 구성 위에서 동작하는 어댑터 독립 진입점이 필요합니다. | [`crates/volicord-core/src/pipeline.rs`](../../../crates/volicord-core/src/pipeline.rs)의 `CoreService`, `CoreService::prepare_request`, `CoreService::execute_prepared_request`, [`crates/volicord-core/src/methods/`](../../../crates/volicord-core/src/methods/)의 메서드 진입 함수. | 공통 사전 점검과 응답 규칙을 중앙화하면서 메서드 파일은 메서드별 계획을 유지합니다. | `CoreService`가 조율 지점이 되므로 공통 사전 점검 변경은 모든 메서드 분기에 영향을 줄 수 있습니다. | [`crates/volicord-core/src/pipeline.rs`](../../../crates/volicord-core/src/pipeline.rs)의 `rejected_branch_has_no_storage_effect`, `committed_mutation_increments_state_version_once`, `operation_category_mismatch_is_rejected_without_effect` 같은 파이프라인 테스트. |
| 재사용 정책 객체 | 메서드마다 순서를 반복하지 않고 접근, Task, 재실행, 최신성, 효과 요구사항을 일관되게 적용해야 합니다. | [`crates/volicord-core/src/pipeline.rs`](../../../crates/volicord-core/src/pipeline.rs)의 `MethodPolicy`, `TaskRequirement`, `ReplayPolicy`, `FreshnessPolicy`, `MethodEffectPolicy`, [`crates/volicord-core/src/methods/mod.rs`](../../../crates/volicord-core/src/methods/mod.rs)의 `mutation_method_policy`. | 메서드 코드는 필요한 정책을 선언하고, 공유 파이프라인은 순서를 담당합니다. | 정책은 공통 관문일 뿐입니다. 메서드별 호환성은 여전히 계획 함수와 참조 담당 문서에 있습니다. | [`crates/volicord-core/src/pipeline.rs`](../../../crates/volicord-core/src/pipeline.rs)의 `stale_expected_state_version_is_rejected_without_effect`, `idempotency_replay_returns_stored_response`. |
| 효과 실행 전 typed 계획 분리 | 공통 결과 사실이 아직 정해지지 않은 상태에서 정상 Core 변이를 커밋하기 전에 메서드가 검증, 메서드별 결과 필드, 이벤트, 저장소 변이를 결정해야 합니다. | [`crates/volicord-types/src/methods.rs`](../../../crates/volicord-types/src/methods.rs)의 `MethodResultFields`와 메서드 결과 선언, [`crates/volicord-core/src/methods/intake.rs`](../../../crates/volicord-core/src/methods/intake.rs)의 `plan_intake`, [`crates/volicord-core/src/methods/prepare_write.rs`](../../../crates/volicord-core/src/methods/prepare_write.rs)의 `plan_prepare_write`, [`crates/volicord-core/src/pipeline.rs`](../../../crates/volicord-core/src/pipeline.rs)의 generic `OwnerPipelineBranch<F>`. | 선언 하나가 필드 전용 계획 타입과 완전한 공개 결과 형태를 함께 제공합니다. 파이프라인은 분기를 선택하는 동안 메서드 필드 타입을 유지하고, 공통 분기 사실이 확정된 뒤에만 완전한 결과를 구성합니다. | 공개 결과 필드를 바꿀 때는 그 선언 하나와 영향을 받는 계획 projection을 갱신해야 합니다. 효과, 상태 버전, 이벤트, 재실행 사실을 결합하는 책임은 공유 파이프라인에 남습니다. | [`crates/volicord-types/src/lib.rs`](../../../crates/volicord-types/src/lib.rs)의 `method_result_fields_compose_every_affected_public_result_schema`, [`crates/volicord-core/src/pipeline.rs`](../../../crates/volicord-core/src/pipeline.rs)의 `no_effect_result_branch_composes_typed_fields_without_storage_effect`, `committed_mutation_increments_state_version_once`, [`crates/volicord-core/src/methods/tests/`](../../../crates/volicord-core/src/methods/tests/) 아래의 메서드 계약 테스트. |
| 명령값처럼 쓰이는 저장소 변이 값 | 메서드 계획 코드가 SQL이나 저장소 순서를 직접 품지 않고 Store에 적용할 내용을 타입으로 전달해야 합니다. | [`crates/volicord-store/src/core_pipeline.rs`](../../../crates/volicord-store/src/core_pipeline.rs)의 `CoreStorageMutation`, `ProjectMutation`, `intake.rs`, `prepare_write.rs`, `record_run.rs` 같은 메서드 계획 코드가 만드는 변이 목록. | Store는 저장 메커니즘을 맡고, Core 계획 코드는 메서드 의도를 맡습니다. | 새 영속 효과를 추가하려면 계획 출력과 Store 변이 적용을 함께 바꾸고, 정확한 동작은 참조 담당 문서에서도 다뤄야 합니다. | [`crates/volicord-store/src/core_pipeline.rs`](../../../crates/volicord-store/src/core_pipeline.rs)의 `task_and_judgment_basis_store_apis_round_trip`, [`crates/volicord-core/src/methods/tests/`](../../../crates/volicord-core/src/methods/tests/) 아래의 메서드 효과 테스트. |
| 명시적 트랜잭션과 원자적 커밋 경계 | 정상 커밋 메서드 효과에서 재실행, 최신성, 상태 버전 변경, 이벤트, 응답 JSON, 저장소 변이가 함께 성공하거나 함께 실패해야 합니다. | [`crates/volicord-store/src/core_pipeline.rs`](../../../crates/volicord-store/src/core_pipeline.rs)의 `CommitMutationInput`, `CommittedMutationFacts`, `MutationCommitOutcome`, `CoreProjectStore::commit_mutation`, [`crates/volicord-store/src/sqlite.rs`](../../../crates/volicord-store/src/sqlite.rs)의 `begin_immediate_transaction`. | 커밋 동작이 여러 부분 쓰기가 아니라 하나의 Store 경계를 갖습니다. | 커밋이 아닌 효과 경로, 특히 일시적 아티팩트 스테이징은 따로 문서화하고 테스트해야 합니다. | Store 트랜잭션 테스트 `transaction_replay_returns_stored_response_before_stale_expected_state`, `transaction_replay_hash_conflict_rejects_without_effect`, 파이프라인 테스트 `committed_mutation_increments_state_version_once`. |
| 주입 가능한 시계 | 권한에 민감한 타임스탬프 동작을 실제 시간 동작을 바꾸지 않고 결정적으로 테스트해야 합니다. | [`crates/volicord-core/src/pipeline.rs`](../../../crates/volicord-core/src/pipeline.rs)의 `Clock`, `SystemClock`, `CoreService::with_clock`, `CoreService::now`, [`crates/volicord-core/src/methods/tests/mod.rs`](../../../crates/volicord-core/src/methods/tests/mod.rs)의 `ManualClock`, [`tests/conformance/baseline.rs`](../../../tests/conformance/baseline.rs)의 `FixedClock`. | 만료, 스테이징, 시간 경계 동작을 예측 가능하게 실행할 수 있습니다. | 시간이 필요한 코드는 시스템 시계를 직접 호출하지 말고 Core를 통해 시간을 받아야 합니다. | [`crates/volicord-core/src/methods/tests/`](../../../crates/volicord-core/src/methods/tests/) 아래의 시간 민감 메서드 테스트와 `CoreService::with_clock`을 쓰는 적합성 시나리오. |
| 불투명 식별자 래퍼 타입 | ID가 문자열로 직렬화되면서도 Rust 안에서는 ID 계열이 구분되어야 합니다. | [`crates/volicord-types/src/ids.rs`](../../../crates/volicord-types/src/ids.rs)의 `opaque_string_type!`와 `ProjectId`, `TaskId`, `RequestHash`, `WriteTicketId` 같은 래퍼. | Rust가 많은 ID 계열 혼동을 잡아 주면서 `serde`는 문서화된 문자열 형태의 API를 유지합니다. | 래퍼 자체가 제품 의미를 검증하지는 않습니다. 검증과 권한은 담당 문서와 구현 점검에 남습니다. | [`crates/volicord-types/src/ids.rs`](../../../crates/volicord-types/src/ids.rs)의 `sequence_generator_preserves_kind_prefixes`, [`crates/volicord-types/src/lib.rs`](../../../crates/volicord-types/src/lib.rs)의 요청 형태 테스트. |
| 열거형 기반 제어 값 | 공개 값 집합과 저장소 쪽 제어 이름을 Rust에서 타입으로 다뤄야 합니다. | [`crates/volicord-types/src/values.rs`](../../../crates/volicord-types/src/values.rs)의 `MethodName`, `OperationCategory`, `ResponseKind`, `EffectKind` 및 관련 열거형. | 어댑터, Core, Store, 테스트가 제어 값을 하나의 Rust 표현으로 공유합니다. | 정확한 값 의미는 여전히 API 값 집합과 메서드 참조 담당 문서에 있습니다. 열거형이 존재한다고 해서 곧바로 제품 계약이 되지는 않습니다. | [`crates/volicord-types/src/lib.rs`](../../../crates/volicord-types/src/lib.rs)의 `typed_requests_derive_documented_operation_categories`와 닫힌 요청 형태 테스트. |
| 정규화된 요청 해시 | 멱등 재실행에는 JSON 속성 순서와 형식에 흔들리지 않는 요청 정체성이 필요합니다. | [`crates/volicord-types/src/canonical.rs`](../../../crates/volicord-types/src/canonical.rs)의 `canonical_json_bytes`, `canonical_json_sha256`, `canonical_request_hash`, [`crates/volicord-core/src/pipeline.rs`](../../../crates/volicord-core/src/pipeline.rs)의 사전 점검 해시 계산. | 재실행된 커밋 호출은 원본 입력 텍스트가 아니라 결정적인 요청 해시로 비교할 수 있습니다. | 해시는 형식화된 요청 표현에 의존하므로 스키마 변경 시 멱등성과 참조 담당 문서를 함께 고려해야 합니다. | [`crates/volicord-types/src/lib.rs`](../../../crates/volicord-types/src/lib.rs)의 `canonical_json_hash_is_order_stable`, `typed_request_hash_ignores_raw_order_and_preserves_semantic_differences`, `typed_request_hashes_are_stable_across_public_request_serialization`, [`crates/volicord-core/src/pipeline.rs`](../../../crates/volicord-core/src/pipeline.rs)의 `idempotency_replay_returns_stored_response`. |
| `volicord-test-support`를 통한 테스트 픽스처 구성 | 계층 간 테스트에 폐기 가능한 Runtime Home 설정, 등록된 프로젝트와 Agent Connection, 요청 빌더가 필요하지만 그 코드를 실제 제품 크레이트에 넣으면 안 됩니다. | [`crates/volicord-test-support/src/lib.rs`](../../../crates/volicord-test-support/src/lib.rs)의 `TempRuntimeHome`, `CoreFixture`, 요청 빌더, Store 검사 도우미. | 테스트가 Runtime Home 데이터를 임시 디렉터리 아래에 두면서 Store, Core, MCP, 적합성 경로를 일관되게 조합할 수 있습니다. | 픽스처 도우미는 설정을 재사용하기 쉽게 만듭니다. 그러나 테스트 단언이 확인하는 제품 의미는 여전히 참조 담당 문서로 돌아가야 합니다. | `tests/conformance/baseline.rs`, `tests/integration/mcp_connection.rs`, [`crates/volicord-test-support/src/lib.rs`](../../../crates/volicord-test-support/src/lib.rs)의 `disposable_runtime_home_stays_under_system_temp` 같은 도우미 테스트. |

## 이 페이지를 사용하는 법

좁은 변경을 수행할 때 이 페이지로 어떤 구현 구조를 건드리는지 확인합니다.
그다음 [구현 가이드](change-guide.md)에서 정확한 참조 담당 문서와 테스트
계층을 찾습니다. 어떤 동작이 패턴에 "맞아 보인다"는 이유로 새 동작을
추가하지 않습니다. 적용되는 담당 문서가 정의했거나 담당 문서 공백이
해결되었을 때만 동작을 추가합니다.
