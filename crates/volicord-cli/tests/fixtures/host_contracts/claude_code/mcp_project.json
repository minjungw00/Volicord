{
  "mcpServers": {
    "volicord": {
      "command": "volicord",
      "args": ["mcp", "--stdio", "--discover-repository", "--host", "claude-code"],
      "env": {"VOLICORD_HOME": "${VOLICORD_HOME}"}
    }
  }
}
